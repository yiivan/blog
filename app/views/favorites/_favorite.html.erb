<% if user_favorite %>
  <%= link_to "Un-favorite", post_favorite_path(@post, user_favorite), method: :delete, remote: true %>
<% else %>
  <%= link_to "Favorite", post_favorites_path(@post), method: :post, remote: true %>
<% end %>

(<%= @post.favorites.count %>)
